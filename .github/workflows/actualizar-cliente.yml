name: Actualización de Cliente Recurrente

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notificación
        run: echo "Se ha actualizado un cliente recurrente. Información enviada al equipo."
